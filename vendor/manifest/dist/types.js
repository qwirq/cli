/** The qwirq.yaml manifest, typed. One source of truth for every consumer (runner, CLI, VS Code);
 *  the JSON Schema sibling lives at schemas/qwirq.schema.json ($id https://qwirq.com/schemas/qwirq.schema.json). */
export {};
